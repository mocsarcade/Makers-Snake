let movementUnits = 5;
export const DIR = {
    UP: [0, -movementUnits], 
    DOWN: [0, movementUnits], 
    LEFT: [-movementUnits, 0], 
    RIGHT: [movementUnits, 0]
};

export default class Direction{}
